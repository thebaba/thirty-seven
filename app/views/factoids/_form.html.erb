<%= form_for(@factoid) do |f| %>
  <% if @factoid.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@factoid.errors.count, "error") %> prohibited this factoid from being saved:</h2>

      <ul>
      <% @factoid.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :summary %><br>
    <%= f.text_field :summary %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit 'Save Factoid', :class => 'btn btn-success' %>
  </div>
<% end %>
